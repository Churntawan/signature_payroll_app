import 'package:flutter/foundation.dart';

void savePngFile(Uint8List bytes, String fileName) {
  // No-op on VM / native tests
}

void saveCsvFile(String content, String fileName) {
  // No-op on VM / native tests
}

